# Template file for 'noctalia-greeter'
# Based on the Voiders Community template (MIT), see packages/fallback/NOTICE.md.
# Voidbleed changes: state directory and first-run setup (INSTALL).
pkgname=noctalia-greeter
version=1.3.0
revision=1
build_style=meson
hostmakedepends="git pkg-config"
makedepends="cairo-devel fontconfig-devel freetype-devel
 json-c++ libepoxy-devel libglvnd-devel librsvg-devel
 libwebp-devel libxkbcommon-devel MesaLib-devel
 pango-devel stb wayland-devel wayland-protocols
 tomlplusplus-devel wlroots0.20-devel"
depends="cage dbus greetd polkit wlr-randr"
short_desc="Noctalia themed greeter for greetd"
maintainer="@MAINTAINER@"
license="MIT"
homepage="https://github.com/noctalia-dev/noctalia-greeter"
distfiles="${homepage}/archive/v${version}.tar.gz"
checksum=e5b8c3ff4b8acadf3760c359e3c7cb655ff92b202c0759f95a34e61012ffcdc5
# greetd creates the _greeter account; the greeter keeps synced appearance here.
make_dirs="/var/lib/noctalia-greeter 0750 _greeter _greeter"

post_install() {
	vlicense LICENSE
}
