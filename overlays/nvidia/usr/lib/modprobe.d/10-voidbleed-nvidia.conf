# Voidbleed NVIDIA profile (installed only when an NVIDIA driver is selected).
#
# The driver packages ship their own nvidia.conf, which blacklists nouveau;
# this file must not be called that or the two collide at install time. What
# is left is what a Wayland session needs from the driver.
options nvidia_drm modeset=1 fbdev=1
