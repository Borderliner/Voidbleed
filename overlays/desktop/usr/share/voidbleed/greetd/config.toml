# Voidbleed greetd config. voidbleed-desktop-config installs this as
# /etc/greetd/config.toml unless that file already points at the Noctalia greeter.

[terminal]
vt = 7

[default_session]
command = "/usr/bin/noctalia-greeter-session"
user = "_greeter"
