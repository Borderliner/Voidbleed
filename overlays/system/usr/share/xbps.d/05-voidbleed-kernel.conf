# Voidbleed pins its kernel series in voidbleed-base (currently linux6.18)
# instead of following Void's "linux" metapackage, which base-system pulls in.
# Other series, including 7.x and linux-mainline, can still be installed by name.
ignorepkg=linux
ignorepkg=linux-headers
