# Voidbleed shows a boot splash, which means plymouth has to be inside the
# initramfs: it draws before the root filesystem is mounted, and it is what
# asks for the passphrase of an encrypted root.
add_dracutmodules+=" plymouth "
